version: '3' 
services:
  web:
    image: flaskapp
    ports:
     - "5000:5000"
    volumes:
     - .:/code
    links:
        - db
    depends_on:
        - db
  db:
    image: mysql
    volumes:
        - /var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: dipto
    ports:
      - "3306:3306"